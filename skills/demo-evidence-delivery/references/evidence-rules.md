# Delivery evidence rules

## Verified now

- FastAPI control plane on `:8081` with a local evidence viewer.
- Optional engine on `:8082`, PostgreSQL project store on `:5433`, and Context
  Bridge in DuckDB plus Markdown.
- Manual refresh of persisted market metrics and protected optional analysis.
- Demo mode and virtual local TP/SL policy when the optional engine runs.

## Optional or conditional

- Bybit demo network access requires a deliberate engine session and local
  credentials.
- GPT/Codex analysis requires an explicit protected request and an available
  configured backend.
- Docker is not required for `make demo`.

## Do not claim

- Live execution, profits, predictions, LLM reasoning, or exchange state that
  was not captured in the shown evidence.
- Qwen usage, SQLite storage, external analytics, automatic agents, or video
  tooling not installed on the workstation.
